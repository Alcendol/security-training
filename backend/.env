# Local development environment variables.
# NEVER commit this file to version control — add .env to .gitignore!

DB_HOST=localhost
DB_PORT=5433
DB_USER=taskuser
DB_PASSWORD=taskpass123
DB_NAME=securetask

# JWT secret: must be at least 32 random characters.
# Generate a new one with: openssl rand -hex 32
JWT_SECRET=a3f8c2e1d94b7056f2a1c3e8d7b4a0f6e2c9d1b8a5f3e0c7d4b2a9f6e3c0d1b8

# Allowed CORS origin (restrict to your actual frontend URL in production)
ALLOWED_ORIGIN=http://localhost:5173

SERVER_PORT=8080
